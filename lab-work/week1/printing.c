#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main() {
        printf("Full name: Sama Al-Oda\n");
        sleep(5);
        printf("Student Number: 400531762\n");
        sleep(5);
        printf("Course name: SFWRENG 2XC3\n");
        sleep(2);
        printf("The most useful thing I've learned so far is the man and --help command, because it can help me find answers to all my questions!\n");
        sleep(2);
        printf("I, personally, do not find setting up VirtualBox useful because I do not use it.\n");
        sleep(2);
        printf("Vim is easy, so it seems faster to use whenever you want to make a quick change.\n");
        printf("On the other side, Vim lacks many of the features that VS Code and other IDE's have.\n");
        sleep(2);
        printf("One advantage of VS Code is that, as an IDE, it offers features like debugging.\n");
        printf("A disadvantage of VS Code is that it requires more storage and power than vim would.\n");
        return EXIT_SUCCESS;
}
